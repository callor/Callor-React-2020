import React from "react";
import PropTypes from "prop-types";

const RSPC = (props) => {
  return (
    <div>
      <h3>RSPC : React Stateless Props</h3>
    </div>
  );
};

RSPC.propTypes = {};

export default RSPC;
